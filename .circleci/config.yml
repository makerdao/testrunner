 version: 2
 jobs:
   build:
     docker:
       - image: levity/dapptools-node
     steps:
       - checkout
       - run:
          command: |
            npm i --no-save yarn
            yarn
       - run:
          command: |
            source /home/circleci/setup-env.sh
            yarn test
